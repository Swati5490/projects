apiVersion: v1
kind: Service
metadata:
  name: {{ include "rewn-chat.fullname" . }}
  labels:
    {{- include "rewn-chat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rewn-chat.selectorLabels" . | nindent 4 }}
