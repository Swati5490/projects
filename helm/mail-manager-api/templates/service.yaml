apiVersion: v1
kind: Service
metadata:
  name: {{ include "mail-manager-api.fullname" . }}
  labels:
    {{- include "mail-manager-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mail-manager-api.selectorLabels" . | nindent 4 }}
