apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "email-api.fullname" . }}-config
  labels:
    {{- include "email-api.labels" . | nindent 4 }}
data:
  # Non-sensitive configuration
  REDIS_HOST: {{ .Values.env | first | .value | quote }}
