apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "account-api.serviceAccountName" . }}
  labels:
    {{- include "account-api.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
