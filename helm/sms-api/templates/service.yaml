apiVersion: v1
kind: Service
metadata:
  name: {{ include "sms-api.fullname" . }}
  labels:
    {{- include "sms-api.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sms-api.selectorLabels" . | nindent 4 }}
